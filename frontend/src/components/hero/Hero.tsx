import React from "react";
import { Link } from "react-router-dom";

interface Props {}

const Hero = (props: Props) => {
    return (
        <section id="hero">
            <h4>Hero Section</h4>
        </section>
    )
};

export default Hero;
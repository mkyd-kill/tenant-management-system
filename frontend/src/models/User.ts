export type UserProfileToken = {
    userName: string;
    email: string;
    token: string;
};

export type UserProfile = {
    userName: string;
    email: string;
};

export type UserRegisterProfile = {
    userName: string;
    email: string;
    password: string;
}